import { EncodeClassPipe } from './encode-class.pipe';

describe('EncodeClassPipe', () => {
  it('create an instance', () => {
    const pipe = new EncodeClassPipe();
    expect(pipe).toBeTruthy();
  });
});
