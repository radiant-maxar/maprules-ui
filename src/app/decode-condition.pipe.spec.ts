import { DecodeConditionPipe } from './decode-condition.pipe';

describe('DecodeConditionPipe', () => {
  it('create an instance', () => {
    const pipe = new DecodeConditionPipe();
    expect(pipe).toBeTruthy();
  });
});
